<%@ page contentType="text/html;charset=UTF-8" language="java" %>
<%@ taglib prefix="c" uri="http://java.sun.com/jsp/jstl/core" %>
<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Chữ Ký Số ElGamal</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" rel="stylesheet">
    <style>
        body { 
            background: linear-gradient(135deg, #667eea, #764ba2); 
            min-height: 100vh; 
            padding: 20px 0; 
            color: #333; 
        }
        .container { max-width: 1400px; }
        .card { 
            border: none; 
            border-radius: 20px; 
            overflow: hidden; 
            box-shadow: 0 10px 30px rgba(0,0,0,0.2); 
        }
        .card-header {
            background: linear-gradient(90deg, #667eea, #764ba2);
            color: white;
            font-weight: bold;
            font-size: 1.25rem;
            padding: 15px 20px;
        }
        .result-box {
            background: #e8f9e8;
            border: 2px solid #c3e6cb;
            border-radius: 12px;
            padding: 15px;
            font-family: 'Courier New', monospace;
            font-size: 1rem;
            margin-top: 15px;
        }
        .error-box {
            background: #fde8e8;
            border: 2px solid #f5c6cb;
            color: #721c24;
            border-radius: 12px;
            padding: 15px;
            margin-top: 15px;
        }
        .history {
            background: #f8f9fa;
            border-radius: 12px;
            padding: 15px;
            max-height: 220px;
            overflow-y: auto;
        }
        .history li {
            padding: 6px 0;
            border-bottom: 1px solid #eee;
        }
        .history li:last-child { border-bottom: none; }
        .btn-primary, .btn-success, .btn-info {
            border-radius: 12px;
            padding: 12px;
            font-weight: bold;
        }
    </style>
</head>
<body>
<div class="container">
    <div class="text-center text-white mb-5">
        <h1 class="display-5 fw-bold">HỆ THỐNG CHỮ KÝ SỐ ELGAMAL</h1>
        <p class="lead">Thực hiện theo thứ tự từ Bước 1 → Bước 2 → Bước 3</p>
    </div>

    <div class="row g-4">
        <!-- BƯỚC 1: TẠO KHÓA -->
        <div class="col-lg-4">
            <div class="card h-100">
                <div class="card-header">
                    <i class="fas fa-key"></i> BƯỚC 1: TẠO KHÓA
                </div>
                <div class="card-body">
                    <form action="keygen" method="post" class="mb-3">
                        <input type="hidden" name="type" value="manual">
                        <div class="mb-3">
                            <label class="form-label fw-bold">Số nguyên tố p (q)</label>
                            <input type="text" class="form-control" name="q" value="19" required>
                        </div>
                        <div class="mb-3">
                            <label class="form-label fw-bold">Số căn nguyên alpha (a)</label>
                            <input type="text" class="form-control" name="a" value="10" required>
                        </div>
                        <div class="mb-3">
                            <label class="form-label fw-bold">Khóa bí mật x</label>
                            <input type="text" class="form-control" name="x" value="5" required>
                        </div>
                        <button type="submit" class="btn btn-primary w-100">Tạo khóa từ dữ liệu vào</button>
                    </form>

                    <form action="keygen" method="post">
                        <input type="hidden" name="type" value="auto">
                        <button type="submit" class="btn btn-success w-100">Tạo khóa ngẫu nhiên</button>
                    </form>

                    <!-- Lỗi tạo khóa -->
                    <c:if test="${not empty sessionScope.keygenError}">
                        <div class="error-box">
                            <strong>Lỗi tạo khóa:</strong><br>
                            ${sessionScope.keygenError}
                        </div>
                        <c:remove var="keygenError" scope="session"/>
                    </c:if>

                    <!-- Hiển thị khóa đúng thập phân -->
                    <c:if test="${not empty sessionScope.keyPair}">
                        <div class="result-box">
                            <strong class="text-success">Khóa Đã Sinh Thành Công!</strong><br>
                            q = ${sessionScope.keyPair.q}<br>
                            a = ${sessionScope.keyPair.a}<br>
                            y = ${sessionScope.keyPair.y}<br>
                            <small class="text-danger">
                                x = ${sessionScope.keyPair.x} (bí mật)
                            </small>
                        </div>
                    </c:if>
                </div>
            </div>
        </div>

        <!-- BƯỚC 2: KÝ TÀI LIỆU -->
        <div class="col-lg-4">
            <div class="card h-100">
                <div class="card-header">
                    <i class="fas fa-signature"></i> BƯỚC 2: KÝ TÀI LIỆU
                </div>
                <div class="card-body">
                    <c:choose>
                        <c:when test="${sessionScope.keyPair != null && sessionScope.keyPair.hasPrivateKey()}">
                            <form action="sign" method="post" enctype="multipart/form-data">
                                <div class="mb-3">
                                    <label class="form-label fw-bold">Văn bản cần ký</label>
                                    <textarea class="form-control" rows="5" name="message" placeholder="Nhập nội dung cần ký tại đây..." required></textarea>
                                </div>
                                <div class="text-center text-muted mb-2">HOẶC</div>
                                <div class="mb-3">
                                    <label class="form-label">Tải lên file</label>
                                    <input type="file" class="form-control" name="file" accept=".txt,.docx,.pdf">
                                </div>
                                <button type="submit" class="btn btn-primary w-100">Ký tài liệu</button>
                            </form>

                            <c:if test="${not empty requestScope.signature}">
                                <div class="result-box mt-3">
                                    <strong class="text-success">Chữ Ký Đã Tạo</strong><br>
                                    ${requestScope.signature}
                                </div>
                            </c:if>
                        </c:when>
                        <c:otherwise>
                            <p class="text-muted text-center fst-italic">
                                Vui lòng tạo khóa ở Bước 1 trước khi ký
                            </p>
                        </c:otherwise>
                    </c:choose>
                </div>
            </div>
        </div>

        <!-- BƯỚC 3: XÁC MINH -->
        <div class="col-lg-4">
            <div class="card h-100">
                <div class="card-header">
                    <i class="fas fa-shield-alt"></i> BƯỚC 3: XÁC MINH CHỮ KÝ
                </div>
                <div class="card-body">
                    <form action="verify" method="post">
                        <div class="mb-2"><input type="text" class="form-control form-control-sm" name="q" placeholder="q (hex)" required></div>
                        <div class="mb-2"><input type="text" class="form-control form-control-sm" name="a" placeholder="a (hex)" required></div>
                        <div class="mb-2"><input type="text" class="form-control form-control-sm" name="y" placeholder="y (hex)" required></div>
                        <div class="mb-2"><input type="text" class="form-control form-control-sm" name="r" placeholder="r (hex)" required></div>
                        <div class="mb-2"><input type="text" class="form-control form-control-sm" name="s" placeholder="s (hex)" required></div>
                        <div class="mb-3">
                            <textarea class="form-control" rows="4" name="message" placeholder="Dán nội dung văn bản gốc..." required></textarea>
                        </div>
                        <button type="submit" class="btn btn-info text-white w-100">Xác minh chữ ký</button>
                    </form>

                    <c:if test="${not empty requestScope.valid}">
                        <div class="result-box mt-3 ${requestScope.valid ? '' : 'error-box'}">
                            <strong class="${requestScope.valid ? 'text-success' : 'text-danger'}">
                                ${requestScope.valid ? 'CHỮ KÝ HỢP LỆ' : 'CHỮ KÝ KHÔNG HỢP LỆ'}
                            </strong>
                        </div>
                    </c:if>
                </div>
            </div>
        </div>
    </div>

    <!-- LỊCH SỬ -->
    <div class="row mt-4">
        <div class="col-12">
            <div class="card">
                <div class="card-body">
                    <div class="d-flex justify-content-between align-items-center mb-3">
                        <h5 class="mb-0"><i class="fas fa-history"></i> Lịch Sử Hành Động</h5>
                        <a href="delete" class="btn btn-danger">Xóa tất cả dữ liệu</a>
                    </div>
                    <c:choose>
                        <c:when test="${not empty sessionScope.history}">
                            <ul class="history list-unstyled mb-0">
                                <c:forEach var="item" items="${sessionScope.history}">
                                    <li>${item}</li>
                                </c:forEach>
                            </ul>
                        </c:when>
                        <c:otherwise>
                            <p class="text-muted mb-0">Chưa có hành động nào.</p>
                        </c:otherwise>
                    </c:choose>
                </div>
            </div>
        </div>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>