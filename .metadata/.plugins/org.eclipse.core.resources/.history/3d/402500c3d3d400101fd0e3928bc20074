@charset "UTF-8";

body {
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    background-color: #f4f6f9; /* Màu nền xám xanh nhạt */
    margin: 0;
    padding: 20px;
}

.container {
    max-width: 1400px;
    margin: 0 auto;
}

/* Header tiêu đề to */
h1 {
    text-align: center;
    color: #333;
    margin-bottom: 5px;
}

.subtitle {
    text-align: center;
    color: #777;
    font-size: 14px;
    margin-bottom: 30px;
}

/* Layout chia 3 cột */
.grid-container {
    display: grid;
    grid-template-columns: 1fr 1fr 1fr; /* Chia đều 3 phần */
    gap: 20px; /* Khoảng cách giữa các cột */
}

/* Nếu màn hình nhỏ (mobile) thì chuyển thành 1 cột */
@media (max-width: 900px) {
    .grid-container {
        grid-template-columns: 1fr;
    }
}

/* Khung từng bước (Card) */
.card {
    background: #fff;
    border-radius: 8px;
    box-shadow: 0 2px 5px rgba(0,0,0,0.05);
    overflow: hidden;
    display: flex;
    flex-direction: column;
    border: 1px solid #e0e0e0;
}

/* Tiêu đề tím của từng bước */
.card-header {
    background: #6a5acd; /* Màu tím đậm giống ảnh mẫu */
    color: #fff;
    padding: 15px;
    text-align: center;
    font-weight: bold;
    text-transform: uppercase;
    letter-spacing: 1px;
}

.card-body {
    padding: 20px;
    flex-grow: 1;
}

/* Các ô nhập liệu */
label {
    display: block;
    margin-bottom: 5px;
    font-weight: 600;
    color: #555;
    font-size: 13px;
}

input[type="text"], textarea {
    width: 100%;
    padding: 10px;
    margin-bottom: 15px;
    border: 1px solid #ddd;
    border-radius: 4px;
    box-sizing: border-box;
    font-family: monospace; /* Font chữ máy tính cho dễ nhìn khóa */
}

textarea {
    resize: vertical;
    min-height: 80px;
}

/* Nút bấm (Buttons) */
.btn {
    border: none;
    padding: 10px 20px;
    border-radius: 4px;
    cursor: pointer;
    color: #fff;
    font-weight: bold;
    width: 100%;
    margin-bottom: 10px;
    transition: 0.2s;
    font-size: 14px;
}

.btn:hover {
    opacity: 0.9;
}

/* Màu từng loại nút */
.btn-primary { background-color: #4361ee; } /* Xanh dương */
.btn-success { background-color: #2ec4b6; } /* Xanh ngọc */
.btn-secondary { background-color: #6c757d; } /* Xám */
.btn-danger { background-color: #dc3545; } /* Đỏ */

/* Phần upload file */
.file-upload-wrapper {
    display: flex;
    gap: 10px;
    margin-bottom: 15px;
    font-size: 12px;
}

/* Khung hiển thị kết quả */
.result-box {
    background: #f8f9fa;
    border: 1px dashed #ccc;
    padding: 10px;
    font-size: 12px;
    color: #333;
    margin-top: 10px;
    word-break: break-all; /* Tự xuống dòng nếu khóa quá dài */
}

/* Thông báo lỗi/thành công */
.alert {
    padding: 10px;
    border-radius: 4px;
    margin-bottom: 15px;
    font-size: 14px;
}
.alert-success { background-color: #d4edda; color: #155724; border: 1px solid #c3e6cb;}
.alert-danger { background-color: #f8d7da; color: #721c24; border: 1px solid #f5c6cb;}

/* Footer bên dưới */
.footer-actions {
    margin-top: 20px;
    text-align: right;
}